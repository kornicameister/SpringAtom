angular.module('sg.app.view.dashboard').config(function ($translateProvider) {
    $translateProvider.translations('pl', {
        'DASHBOARD_PICK_TAB_MSG': 'Proszę wybrać jedną z zakladek po prawej, aby rozpocząć pracę'
    });
});